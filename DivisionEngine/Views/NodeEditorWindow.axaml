<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:DivisionEngine.Editor.ViewModels"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="600"
             x:Class="DivisionEngine.Editor.NodeEditorWindow"
             >
    
    <!--
    <Grid Background="#2A2A2A">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#3A3A3A" Height="40">
            <StackPanel Orientation="Horizontal" Margin="10,0">
                <Button Content="Add Float" 
                        Command="{Binding AddFloatCommand}"
                        Background="#555" Foreground="White"
                        Padding="10,5"/>
            </StackPanel>
        </Border>

        <ScrollViewer Grid.Row="1" Background="#252525">
            <ItemsControl ItemsSource="{Binding Nodes}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas Background="Transparent"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="#3A3A3A" 
                                BorderBrush="#555" 
                                BorderThickness="1"
                                CornerRadius="4"
                                Width="150" Height="60">
                            <Canvas.Top>
                                <Binding Path="Y"/>
                            </Canvas.Top>
                            <StackPanel Margin="8">
                                <TextBlock Text="{Binding Name}" 
                                           Foreground="White" 
                                           FontWeight="Bold"
                                           FontSize="12"/>
                                <TextBlock Text="{Binding Value}" 
                                           Foreground="#AAA" 
                                           FontSize="10"
                                           IsVisible="{Binding Value, Converter={x:Static ObjectConverters.IsNotNull}}"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
    -->
</UserControl>